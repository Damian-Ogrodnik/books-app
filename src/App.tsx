import { BooksContainer } from './features/books/containers/BooksContainer';

export const App = () => (
  <div className="App">
    <header className="App-header">
      <BooksContainer />
    </header>
  </div>
);
